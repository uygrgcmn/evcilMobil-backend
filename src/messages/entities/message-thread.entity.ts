export type MessageThread = {
  id: string;
  senderName: string;
  avatarUrl: string;
  previewText: string;
  timeLabel: string;
  unreadCount: number;
  isOnline: boolean;
  isPriority: boolean;
};
